targets:
  $default:
    builders:
      drift_dev:
        options:
          # ISO-8601 text, not unix seconds. LWW compares client_edited_at
          # between devices and second precision loses real conflicts
          # (spec 10.2).
          store_date_time_values_as_text: true
          named_parameters: true
